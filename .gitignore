# Build artifacts
*.bc
*.ll
*.o
*.out

# Isabelle
*.thy~
.isabelle/
output/
heaps/
browser_info/

# SAW
*.saw-cache/
saw-out/

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# Local toolchain
/.tools/

# paper/ is a PRIVATE submodule (paper drafts + working notes) -> github.com/amarshat/foolscap
# (access-controlled). Managed via .gitmodules, not gitignored.
implementations/**/target/
implementations/rustcrypto-ml-dsa/build/
*.linked-mir.json

# Scratch proof logs / experimental saw runs (underscore-prefixed)
implementations/**/proof/**/_*.log
implementations/**/proof/**/_*.saw
proof/**/_*.log
proof/**/_*.saw
qseal/model/_*.cry
qseal/build/
qseal/demo/target/
cap/target/

# Blog drafts / generated images (kept local; public repo, private framing)
**/BLOG_*.md
**/blog_*.png

# Local tooling / agent dirs
/.agents/
/.kiro/
/build/
/skills-lock.json

# Local throwaway Isabelle scratch sessions (fast-iteration heaps), never committed.
/scratchpad/
cap/demo/target/

# local agent/config material: never ship it with the artifact
.claude/
__pycache__/
