# Build artifacts
*.bc
*.ll
*.o
*.out

# Isabelle
*.thy~
.isabelle/
output/
heaps/
browser_info/

# SAW
*.saw-cache/
saw-out/

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# Local toolchain
/.tools/

# Paper kept local until publication (sources, build artifacts, submission notes).
# The public artifact is the proofs/code, not the paper draft.
/paper/
implementations/**/target/
implementations/rustcrypto-ml-dsa/build/
*.linked-mir.json

# Scratch proof logs / experimental saw runs (underscore-prefixed)
implementations/**/proof/**/_*.log
implementations/**/proof/**/_*.saw
proof/**/_*.log
proof/**/_*.saw
qseal/model/_*.cry

# Blog drafts / generated images (kept local; public repo, private framing)
**/BLOG_*.md
**/blog_*.png

# Local tooling / agent dirs
/.agents/
/.kiro/
/build/
/skills-lock.json

# Local throwaway Isabelle scratch sessions (fast-iteration heaps), never committed.
/scratchpad/
